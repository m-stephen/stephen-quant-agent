# V11.21 冻结模型续验：B18 工程状态

截至 2026-09-07，本次只完成合成数据开发验证，没有执行新的市场实验，也没有可用 Alpha 结论。真实 Trial 累计下界仍为 **3707**。

## 已完成

- 新建 `flow_response_continuation.py`，完整预留 22 个原生账户 Trial 后才读取继承模型和数据。
- 继承原始只读登记库、冻结历史矩阵、14 个年度模型及其 28 个成本账户绑定，不重新拟合，不伪造新 fit 记录。
- 原模型预测继续通过原始登记库的时间和血缘检查。已保存的目标仓位须一致；原始两个基准保留文件字节。
- 统一运行两种成本下的全部候选和对照，逐账户独立核算；失败保留全部预留和失败文件，禁止同一运行重试。
- 新数据库用 snapshot 绑定继承证据和此前失败尝试；原数据库哈希前后不变。
- 全部运行结束仍是 `COMPLETE_PENDING_INDEPENDENT_AUDIT`，不能称为 Alpha Court 通过。

## 合成测试

- 续验边界测试：12 passed。
- 完整历史生成、冻结续验与原审计测试组：14 passed，210.08 秒。
- 其中一次续验实际完成 22 个合成账户，和原始合成运行逐项比较指标、账户审计、紧凑账户文件 SHA-256，全部一致；生产拟合函数被测试替换为禁止调用。
- CI 结果校验器：11 passed；Ruff 通过。
- 此前针对原生崩溃的 Windows 复现组：34 passed，162.71 秒；不能据此声明 Linux 崩溃已修复。

## CI 故障与固定对照

[原 CI](https://github.com/m-stephen/stephen-quant-agent/actions/runs/34118784744) 在完整合成账户测试内发生 SIGSEGV，未生成完整 JUnit。
[带 GDB 的诊断](https://github.com/m-stephen/stephen-quant-agent/actions/runs/34120458298) 再次捕捉到 SIGSEGV：原生栈含 `PyObject_Hash`、`tuplehash`、`_PyDict_LoadGlobal`，紧接 120 秒定时栈输出。

这不是普通断言失败，具体根因仍未证明。诊断脚本保留非零退出、原生栈和资源日志，缺失或失败的 JUnit 不能变成成功。

固定的下一次对照只比较 Python3.10/定时输出120秒、Python3.10/定时输出关闭、Python3.12/定时输出120秒；全部运行同一完整测试集，原组仍为必需组。关闭定时输出不等于跳过断言或关闭致命故障检测。尚不能据未完成的结果决定迁移运行环境。

## 尚未完成，禁止真实启动

1. 新续验运行专用的独立 source → 特征 → 成熟标签/模型 → 仓位 → 订单/现金/NAV 全链路审计。
2. 固定实际代码、历史失败证据、B17 诊断及输入哈希的启动计划，以及共享原子 claim、GitHub 预注册校验、受限子进程。
3. 上述完整版本的本机测试与 CI。
4. 才可预注册并一次性执行真实 22 账户续验；届时累计下界为 3729。**目前并未预留这 22 个真实 Trial。**

旧失败运行、B17 原始 UNRESOLVED 状态不改写。候选、成本、收益筛选和 Court 门槛不改；不读取 2025–2026 做调参，不合并 main、不交易、不新增数据购买。
# 后续进展

B18 后已实现独立审计，当前状态见 [B19 审计说明](V11_21_CONTINUATION_AUDIT.zh.md)。下文保留 B18 历史记录；实证 launcher 仍待完成。
