# V9.1 全量分钟物化结果

状态：**通过本机数据、重放、完整性、测试、Ruff 与 GitHub Actions 门禁。**

## 全量结果

| 指标 | 结果 |
|---|---:|
| 可识别分钟归档 | 194 |
| MATERIALIZED 归档 | 194 |
| 来源成员 | 4,447,591 |
| 物理 Parquet 分区 | 1,460 |
| 最终快照 | `fde4ef3e...17dfb` |
| 全量验证行数 | 6,340,365,218 |
| current 重复主键 | 0 |
| PIT 时序违规 | 0 |
| stderr | 0 bytes |

启动时存在 31 个 AVAILABLE 和 2 个 PARTIAL 归档。本轮使用 8 个解析 worker、父进程单写
DuckDB，新增 103,615 个成员、3,829,898,531 条修订和 420 个分区。原始目录始终只读。

## 存储与安全

- V9.0.1 将既有 1,040 个分区从 123.43 GiB 压缩到 28.50 GiB，下降 76.91%。
- V9.1 新增范围分区后，范围 Parquet 为 71.85 GiB，日分区为 3.58 GiB。
- 本轮物化净消耗约 45.79 GiB；结束后 E 盘仍有约 418.28 GiB 空闲，高于 100GB 门槛。
- 3 个未被 Catalog、迁移 ledger 或任何快照引用的 orphan 经逐项证明后删除，释放约
  1.34 GiB；清理后物理 Parquet 数与 Catalog 均为 1,460。
- 4,818,223 条未满足解析契约的源行保留在 quarantine 统计中，没有伪造或静默填充。

## 重放与验证

第二次运行返回 `pending_archives_at_start=0`、`archive_results=[]`、`estimated_parquet_bytes=0`，
最终快照 ID 不变。流式验证逐文件检查 SHA-256，并扫描全部 6,340,365,218 行；结果为
`passed=true`、重复主键 0、PIT 时序违规 0。

## 结论

V9.1 已完成本机数据门禁。它提升数据可用性和自动研究吞吐，但本身不构成 Alpha 证据；
下一阶段是在该仓库上建设分钟特征层、跨源因子语法和统一 Alpha Court。
