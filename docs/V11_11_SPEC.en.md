# V11.11 Temporal increments within a low-risk foundation

Test three fixed20-session path summaries, not amendments to the frozen chip lead:
flow sign consistency times absolute net/gross flow; signed auction cubic mass
divided by absolute cubic mass; net chip-width change divided by total absolute
path change. All-zero denominators produce0; missing source observations never do.
Stock/session gaps reset the20-consecutive-market-session requirement. All policies
share coverage. Prior code/catalogues already contain flow means, mean differences,
chip instantaneous changes and related interactions; these path-shape formulas
are different, not statistically independent of that research history.

At each close select the200lowest-volatility names from current common coverage,
ties by instrument. Preserve all execution/exit bars. Centered cross-sectional
ranks and training are computed only within that contemporaneous low-risk pool.
The loader still validates the entire frozen five-source extract, but stale minute
fields are not temporal-event inputs in this experiment.

2023fit uses2022;2024fit uses2022–2023. Slice the prefix before labels, embargo the
final5sessions, sample every5sessions; label next-open to open20sessions later.
Missing entry excluded/count; missing exit=-100% conservative target/count, not
proof of an actual delisting. OLS removes intercept and volatility/momentum/ADV
rank effects from signal and label; fixed residual ridge0.01, numerical diagonal
1e-10. Direction comes from training only. Native fit stages are registered before
reads; all runtime model bytes/dates are bound and verified before allocation.

Keep existing desired names within the low-risk200; fill vacancies in low-vol
order to40names×2.5%. Candidates replace only if the predicted improvement exceeds
the unchanged122bps hurdle. Controls: identical foundation without active swaps;
common-coverage low-vol Top40/buffer10. Mandatory pool migration/fills and aggregate
netting drift can still trade. Four fixed phases0/5/10/15,20-session cadence, one
continuous CNY3m netted account, no annual capital/calendar reset or phase choice.
Keep existing capacity/open constraints; costs41/82/102bps nominal round trip.

Budget:6yearly fits+5policies×3costs=21reserved attempts; debt3289→3310, including
failures/unused reservations. Six fit Trials each bind one stage; nine candidate
accounts each bind both; six controls explicitly unfitted:24native model bindings.
Use only exposed2022–2024;2023/2024remain development, not freshOOS. No2025/2026reads.

Historical lead screen at BOTH82and102bps: positive each year, Sharpe≥0.7,
full-pathMDD≥-25%, total-return increment against EACHcontrol≥3pp, annual increment
against low-vol≥-5pp and account audit pass. Passing is not validated Alpha.
Adaptive-return CPCV is not nested model refitting; fewer than two distinct nonzero
active paths cannot identify meaningful PBO. Local-family DSR sensitivity and block
sign placebo are not full-history/independent certification. Formal DSR≥.95,
PBO≤.05, eachplacebo≤.05 and execution/path/capacity gates remain unchanged.

Preserve old claims/evidence/frozen candidate/main. Reconcile all15accounts and24
native bindings independently and archive Chinese/English JSON/Markdown reports.
Notify only usable Alpha or a real required user action, not routine failed epochs.
