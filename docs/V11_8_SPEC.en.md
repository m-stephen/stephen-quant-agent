# V11.8 Frozen Lead Challenge

Issue #184 freezes both prior formulas, directions, Top40, ten-rank buffer and
20-session holding horizon. Reuse only the immutable historical-development
snapshot for2023–2024; do not read2025/2026 or claim independent holdout evidence.

## Predeclared challenges

Run each lead and its coverage-matched low-vol/hash controls at82bps round-trip
(exact baseline replay),102bps,132bps, quarter ADV capacity, and a one-session
delay of the entire frozen target schedule. Costs are rerun through cash/share
accounting, not subtracted from final returns. Quarter capacity is1.25% of lagged
ADV, NOT evidence of actual opening-auction liquidity.

A sixth scenario carries actual research positions, cash and rebalance calendar
continuously across2023–2024. It is not a synthetic link of annually reset accounts.
Annual attribution within it must use prior-year-end NAV, not reset CNY3m.

Register30 new policy trials (six policies times five new scenarios) before market
reads. Twelve baseline annual accounts must match their original account AND target
hashes and consume zero new trials. Retain the prior lower bound3058, yielding3088
for the first challenge epoch. Failed reservations remain charged; subsequent
operations must include all prior challenge reservation evidence.

## Interpretation

Baseline diagnostics use single-control low-vol OLS with20-lag Bartlett HAC and
an explicitly nontradable counterfactual setting the five largest positive active
days equal to control returns. These are postselection descriptions, not adjusted
significance, strategy changes, or proof of market/industry/size neutrality.

Every annual stress applies the unchanged V11.7 economic screen. Survivors advance
to raw-price brokerage execution and style audits; complete failure advances to a
preregistered bounded mechanism epoch. Continuous results are reported, not used to
invent another selection threshold. Historical evidence never sets validated_alpha.
Adjusted fractional shares, absent board lots/minimum commissions and incomplete
corporate-action accounting still preclude brokerage-readiness claims.

## Continuation and reproducibility

Return a resumable next action: repair engineering, challenge brokerage/style, or
preregister the next bounded mechanism family. The scheduler checks live jobs and
PRs before resuming; it retains failed attempts, frozen Court thresholds and sealed
forward protocols. Independent evidence and complete calibrated Court tests remain
necessary for a usable-Alpha claim. No trading or data purchases are authorized.

Local paths stay in ignored config. Parent results/ledgers, frozen leads and inputs
are protected by hashes. Output uses exclusive creation outside parent/input trees;
failure preserves ABORTED and reservations. Commit only code, synthetic fixtures and
bilingual aggregate conclusions, never raw/private data or per-stock account dumps.
