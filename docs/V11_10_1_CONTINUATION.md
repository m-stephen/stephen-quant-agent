# Issue184 continuation — V11.10.1

Read V11_10_CONTINUATION.md for frozen market evidence and prior lineage. This
patch is an engineering-only dependency after codex/v11.10-residual-mechanisms,
not a new market epoch. Old dirty root and unmerged branches must remain untouched.

## Completed, do not repeat

- Native per-Trial fit contracts, append-only actual-date/model/snapshot/code evidence,
  exact model-byte checks and guarded residual target execution are implemented.
- Full path planted/null/blocked-fill synthetic tests pass, plus backwards-compatible
  schema upgrade on temporary synthetic data. See VERIFICATION and bilingual results.
- Market trial increment0; raw historical lower bound3289 remains, including unused
  and aborted reservations. No real market dataset or warehouse reads in this patch.
- Original V11.10 RESULT6559581264e706f932df02c7ec1cc41e15980501cf6313f80a438337a622e2b2
  and runtime55c4083bedde96f6e46c0faf950dc2b8b38fceacd1b7839e24f9a031118ebce6 preserved
  on their original worktree. Do not reopen frozen registries with initializing APIs.
- PR18797d5a9d CI34038247576passed. Check this patch's own PR and CI live.
- The old V11.10 workflow is CLOSED and deliberately not retrofitted or rerun.
  New research must directly use native TrialSpec.fit_stages and guarded execution;
  a legacy empty/missing contract is not verified model lineage.

## Next bounded research

First inspect live Python processes and existing claims/results/PRs; no duplicate
execution. Do not create another infrastructure-only cycle for the same solved gap.
If this patch's CI passes, use an independent codex/ branch for a finite new epoch.
Preserve the V11.9 staggered-chip observation, with its cost/statistical limitations.

Focus on incremental signal within a fixed low-risk foundation, not the losing
all-risk stratified portfolio. Existing panel fields permit temporal changes and
persistence in daily fund-flow/auction and daily chip-width inputs. Temporal
features must use only prior/as-of sessions, explicit gaps and coverage; minute
features can be up to7calendar days stale, so do not silently call them fresh
minute events. Inspect previous registered/tombstoned hypotheses before defining
at most a few genuinely distinct mechanisms. Do not relabel failed static
interactions or tune their122bps hurdle, signs, ridge or calendar phase on exposed data.

Before any market read publish the exact new mechanism, allowed transforms, direction
or training-only fit, hold horizon, purge/embargo, universe, base allocation,
replacement rules, cost/capacity cases, economic criteria and full fit/account
reservation budget in Issue184. Start debt3289 and add every attempt, including
controls, failed fits and unused reservations. New parent/operation/claim identity;
never reuse V11.10's old parent debt3268 or delete a claim to restart.

If fitting models, register native stages atomically before fitting; save actual
fit artifacts, bind each fit/account Trial and use guarded_targets (or an equally
tested native guard adapted to the new mechanisms) before any target calculation.
Carry complete execution ledgers and independently reconcile all accounts. Store
ordinary failed results in Chinese/English and JSON without claiming Alpha.

2022–2024 are reused development, not fresh OOS; 2025/2026 remain under existing
sealed/one-time protocols. Do not lower Court DSR>=.95, PBO<=.05, eachplacebo<=.05,
path/cost/capacity gates. Adaptive return-matrix CPCV is not nested model refitting,
and current-family sensitivity is not full-history DSR. Brokerage execution and
truly independent evidence still limit any deployable-Alpha claim.

Continue the existing30minute heartbeat. No new standalone task or main merge,
no data purchase, broker/trading action, private paths/raw-data/credential commits.
Quiet ordinary failures; notify only usable Alpha, an actual user-action blocker
or major fault. Never promise that iteration must discover Alpha.
